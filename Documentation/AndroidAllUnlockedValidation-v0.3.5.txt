Android all-content-unlocked update - 2026-09-11

APK: Builds/Android/CozySurvivors-v0.3.5.apk
Package: com.secondwindgames.cozysurvivors
Version: 0.3.5 (code 8)
Minimum SDK: 26 / Target SDK: 36 / ABI: arm64-v8a
Non-development build / debug signing for direct installation
Bytes: 63660351
SHA-256: B1332FBA3C5D1FB45683FD3DE34117F51C2E7FD83C7DAFB90736694ED2BF2573

PASS Android build succeeded with BuildOptions.None, 0 errors
Build warnings: 971 package/shader warnings
PASS 141 regression checks for simulation, saves, rewards and content access
PASS 4 checks executed against the actual stripped Android release assembly:
  all content on fresh profiles without initialization;
  all content with previous configured OFF preferences;
  invalid IDs rejected;
  included decorations never charge stars.
See AndroidReleaseUnlockChecks-v0.3.5.txt for the managed release checks.
PASS APK metadata confirms version code 8, version name 0.3.5, SDKs and ARM64
PASS APK manifest is not debuggable
PASS APK Signature Scheme v2 verification
PASS signing certificate matches v0.3.4 for an in-place direct APK update
PASS zipalign -c -P 16 4
PASS copied APK SHA-256 equals the isolated build artifact

The content gates no longer use UNITY_EDITOR, DEVELOPMENT_BUILD or saved
testUnlockAll preferences. The Settings screen shows All content unlocked
and has no OFF toggle. Actual clear, achievement, purchase and reward data
is preserved. Combat and run progression are unchanged.

The user chose to build the AAB themselves. The project and configured-signing
AAB build command are updated; the existing root W05.aab was not replaced.
No Google Play upload or physical Android device testing was performed.
